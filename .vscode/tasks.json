{
	"version": "2.0.0",
	"tasks": [
		{
			"label": "Package: test",
			"command": "python",
			"args": ["-m ${workspaceFolder}/test pytest --doctest-modules ${workspaceFolder}/grade_conversion_script"],
			"detail": "Run pytest",
			"group": {
				"kind": "test",
				"isDefault": true
			}
		}
	]
}